# 语音生成表格快捷菜单完善计划

## 一、当前状态分析

### 1.1 "清空已完成任务"菜单项

| 项目 | 当前状态 |
|------|----------|
| 菜单项定义 | ✅ 已定义 (`清空已完成任务NToolStripMenuItem`) |
| Click事件绑定 | ❌ **未绑定** |
| 启用/禁用逻辑 | ❌ **未实现** |

### 1.2 "清空所有任务"菜单项

| 项目 | 当前状态 |
|------|----------|
| 菜单项定义 | ✅ 已定义 (`清空所有任务AToolStripMenuItem`) |
| Click事件绑定 | ✅ 已绑定 |
| 启用/禁用逻辑 | ✅ 已实现 (`totalRows > 0`) |

**结论**："清空所有任务"菜单项的启用条件已正确实现（表格中有任务时启用），无需修改。

---

## 二、需要完成的修改

### 2.1 修改文件清单

| 文件 | 修改内容 |
|------|----------|
| [VoiceGenerationPanel.Designer.cs](file:///d:/User%20Data/guanw@hotmail.com/我的私有云/GW's%20Data/我的开发/GWs/TTL工具箱/TTL%20Tools%20Box%20V1.0/GW.TTLtoolsBox.WinFormUi/UI/Panels/VoiceGenerationPanel.Designer.cs) | 绑定Click事件 |
| [VoiceGenerationPanel.cs](file:///d:/User%20Data/guanw@hotmail.com/我的私有云/GW's%20Data/我的开发/GWs/TTL工具箱/TTL%20Tools%20Box%20V1.0/GW.TTLtoolsBox.WinFormUi/UI/Panels/VoiceGenerationPanel.cs) | 添加启用逻辑和事件处理方法 |

---

## 三、具体实现步骤

### 步骤1：绑定Click事件（Designer.cs）

在 `VoiceGenerationPanel.Designer.cs` 的菜单项属性设置区域添加事件绑定：

```csharp
this.清空已完成任务NToolStripMenuItem.Click += new System.EventHandler(this.清空已完成任务NToolStripMenuItem_Click);
```

**位置**：约第386行，在现有属性设置之后

---

### 步骤2：添加启用/禁用逻辑（VoiceGenerationPanel.cs）

在 `cms_语音生成_任务控制_Opening` 方法中添加：

```csharp
清空已完成任务NToolStripMenuItem.Enabled = _voiceGenerationTaskQueue.Tasks.Any(t => t.Status == VoiceGenerationTaskStatus.已完成);
```

**位置**：约第574行，在 `清空所有任务AToolStripMenuItem.Enabled` 设置之后

---

### 步骤3：添加Click事件处理方法（VoiceGenerationPanel.cs）

在 `清空所有任务AToolStripMenuItem_Click` 方法之后添加新方法：

```csharp
/// <summary>
/// 事件处理：点击"清空已完成任务"菜单项。
/// </summary>
private void 清空已完成任务NToolStripMenuItem_Click(object sender, EventArgs e)
{
    DialogResult result = MessageBox.Show(
        "确定要清空所有已完成的任务吗？",
        "确认",
        MessageBoxButtons.YesNo,
        MessageBoxIcon.Question,
        MessageBoxDefaultButton.Button2);

    if (result == DialogResult.Yes)
    {
        clearCompletedTasksAndCleanup();
    }
}
```

---

### 步骤4：添加清空已完成任务的辅助方法（VoiceGenerationPanel.cs）

在 `clearAllTasksAndCleanup` 方法之后添加：

```csharp
/// <summary>
/// 清空所有已完成任务并清理相关资源。
/// </summary>
private void clearCompletedTasksAndCleanup()
{
    var completedTasks = _voiceGenerationTaskQueue.Tasks
        .Where(t => t.Status == VoiceGenerationTaskStatus.已完成)
        .ToList();

    foreach (var task in completedTasks)
    {
        if (task.Items != null)
        {
            foreach (var item in task.Items)
            {
                if (!string.IsNullOrWhiteSpace(item.TempFile) && File.Exists(item.TempFile))
                {
                    try
                    {
                        File.Delete(item.TempFile);
                    }
                    catch
                    {
                    }
                }
            }
        }
    }

    foreach (var task in completedTasks)
    {
        _voiceGenerationTaskQueue.Tasks.Remove(task);
    }

    update语音生成选中行信息();
}
```

---

## 四、修改位置汇总

| 文件 | 行号范围 | 修改类型 |
|------|----------|----------|
| VoiceGenerationPanel.Designer.cs | ~386 | 添加事件绑定 |
| VoiceGenerationPanel.cs | ~574 | 添加启用逻辑 |
| VoiceGenerationPanel.cs | ~685后 | 添加Click事件处理方法 |
| VoiceGenerationPanel.cs | ~1269后 | 添加辅助方法 |

---

## 五、功能验证清单

- [ ] 表格中无"已完成"任务时，"清空已完成任务"菜单项禁用
- [ ] 表格中有"已完成"任务时，"清空已完成任务"菜单项启用
- [ ] 点击"清空已完成任务"弹出确认对话框
- [ ] 确认后只删除"已完成"状态的任务
- [ ] 删除时清理对应的临时文件
- [ ] 其他状态的任务不受影响
