# 实现"发送当前段落到下一步"功能计划

## 任务背景
用户在"角色和情绪指定"面板添加了"发送当前段落到下一步"按钮，需要实现该按钮的功能：仅发送当前显示在表格里的段落到下一步，与现有的"发送所有段落到下一步"按钮功能区分。

## 现有代码分析

### 相关文件
- [RoleEmotionPanel.cs](file:///d:/User%20Data/guanw@hotmail.com/我的私有云/GW's%20Data/我的开发/GWs/TTL工具箱/TTL%20Tools%20Box%20V1.0/GW.TTLtoolsBox.WinFormUi/UI/Panels/RoleEmotionPanel.cs)

### 现有关键代码

1. **按钮定义**（已在Designer中定义）：
   - `bt_发送所有段落到下一步` - 已实现
   - `bt_发送当前段落到下一步` - 需要实现事件处理

2. **现有事件绑定**（第265-266行）：
   ```csharp
   this.bt_发送所有段落到下一步.Click -= bt_发送到下一步_Click;
   this.bt_发送所有段落到下一步.Click += bt_发送到下一步_Click;
   ```

3. **现有发送方法**（第944-949行）：
   ```csharp
   private void bt_发送到下一步_Click(object sender, EventArgs e)
   {
       string finalText = generateFinalText();
       OnSwitchToNextPageRequested(finalText);
   }
   ```

4. **生成所有段落文本的方法**（第750-806行）：
   - `generateFinalText()` - 遍历 `_allParagraphsData` 生成所有段落文本

5. **关键数据结构**：
   - `_allParagraphsData` - 所有段落的数据列表
   - `_currentParagraphIndex` - 当前显示的段落索引
   - `_roleTextItems` - 当前显示的段落数据

## 实现方案

### 方案：重构提取公共方法

将段落文本生成逻辑提取为独立的私有方法，两个按钮共用该核心逻辑。

#### 步骤1：创建生成单个段落文本的方法
```csharp
/// <summary>
/// 生成指定段落的文本。
/// </summary>
/// <param name="paragraphItems">段落的角色文本项列表。</param>
/// <returns>生成的段落文本。</returns>
private string generateParagraphText(List<RoleTextItem> paragraphItems)
```

#### 步骤2：重构 `generateFinalText()` 方法
调用 `generateParagraphText()` 生成每个段落的文本，避免代码重复。

#### 步骤3：创建生成当前段落文本的方法
```csharp
/// <summary>
/// 生成当前段落的文本。
/// </summary>
/// <returns>生成的当前段落文本。</returns>
private string generateCurrentParagraphText()
```

#### 步骤4：创建"发送当前段落到下一步"按钮的事件处理方法
```csharp
/// <summary>
/// 事件处理：点击"发送当前段落到下一步"按钮。
/// </summary>
/// <param name="sender">发送者。</param>
/// <param name="e">事件参数。</param>
private void bt_发送当前段落到下一步_Click(object sender, EventArgs e)
{
    string currentText = generateCurrentParagraphText();
    OnSwitchToNextPageRequested(currentText);
}
```

#### 步骤5：绑定按钮事件
在 `init角色和情绪指定Ui()` 方法中添加事件绑定：
```csharp
this.bt_发送当前段落到下一步.Click -= bt_发送当前段落到下一步_Click;
this.bt_发送当前段落到下一步_Click += bt_发送当前段落到下一步_Click;
```

#### 步骤6：更新UI刷新方法
在 `refresh角色和情绪指定Ui()` 方法中添加按钮启用状态设置：
```csharp
this.bt_发送当前段落到下一步.Enabled = hasData;
```

## 修改文件清单

| 文件 | 修改内容 |
|------|----------|
| RoleEmotionPanel.cs | 1. 新增 `generateParagraphText()` 方法<br>2. 重构 `generateFinalText()` 方法<br>3. 新增 `generateCurrentParagraphText()` 方法<br>4. 新增 `bt_发送当前段落到下一步_Click()` 事件处理方法<br>5. 在 `init角色和情绪指定Ui()` 中绑定事件<br>6. 在 `refresh角色和情绪指定Ui()` 中设置按钮启用状态 |

## 代码位置参考

- `generateFinalText()` 方法：第750-806行
- `init角色和情绪指定Ui()` 方法：第256-288行
- `refresh角色和情绪指定Ui()` 方法：第297-304行
- 事件绑定区域：第262-284行
