# 修改保存文件功能调整计划

## 一、需求概述

将"语音生成"表格快捷菜单项"修改保存文件夹"更改为"修改保存文件"，功能从仅修改保存文件夹调整为允许同时修改保存文件夹和文件名。

## 二、当前代码分析

### 2.1 涉及文件
| 文件 | 说明 |
|------|------|
| `GW.TTLtoolsBox.WinFormUi\UI\Panels\VoiceGenerationPanel.Designer.cs` | 菜单项控件定义 |
| `GW.TTLtoolsBox.WinFormUi\UI\Panels\VoiceGenerationPanel.cs` | 事件处理逻辑 |

### 2.2 当前实现
- **控件名称**：`修改保存文件MToolStripMenuItem`（已符合要求）
- **菜单文本**：`修改保存文件(&M)...`（已符合要求）
- **事件处理方法**：`修改存储文件夹MToolStripMenuItem_Click`（需重命名）
- **当前功能**：使用 `FolderBrowserDialog` 选择文件夹，保留原文件名
- **当前启用条件**：`!hasPreviewTask && !anyConvertingMp3 && !allMp3ConversionFailed`

### 2.3 任务状态枚举（VoiceGenerationTaskStatus）
- `未开始` - 目标有效状态
- `排队中` - 目标有效状态
- `正在生成` - 无效状态
- `已完成` - 无效状态
- `生成失败` - 无效状态
- `转换MP3` - 无效状态
- `转换MP3失败` - 无效状态

## 三、修改方案

### 3.1 修改 VoiceGenerationPanel.Designer.cs

**位置**：第330行

**修改内容**：将事件处理方法名从 `修改存储文件夹MToolStripMenuItem_Click` 改为 `修改保存文件MToolStripMenuItem_Click`

```csharp
// 修改前
this.修改保存文件MToolStripMenuItem.Click += new System.EventHandler(this.修改存储文件夹MToolStripMenuItem_Click);

// 修改后
this.修改保存文件MToolStripMenuItem.Click += new System.EventHandler(this.修改保存文件MToolStripMenuItem_Click);
```

### 3.2 修改 VoiceGenerationPanel.cs - 菜单启用条件

**位置**：第643行附近

**修改内容**：修改菜单启用条件，仅当选择单个任务且状态为"未开始"或"排队中"时启用

```csharp
// 修改前
修改保存文件MToolStripMenuItem.Enabled = !hasPreviewTask && !anyConvertingMp3 && !allMp3ConversionFailed;

// 修改后
bool canModifySaveFile = isSingleSelection && 
    (selectedTasks[0].Status == VoiceGenerationTaskStatus.未开始 || 
     selectedTasks[0].Status == VoiceGenerationTaskStatus.排队中);
修改保存文件MToolStripMenuItem.Enabled = canModifySaveFile;
```

### 3.3 修改 VoiceGenerationPanel.cs - 事件处理方法

**位置**：第1010-1056行

**修改内容**：重写 `修改存储文件夹MToolStripMenuItem_Click` 方法为 `修改保存文件MToolStripMenuItem_Click`

**新实现逻辑**：
1. 验证选中任务数量（必须为1个）
2. 获取选中任务
3. **验证任务状态**（必须为"未开始"或"排队中"），若不符合则弹出提示框并返回
4. 使用 `SaveFileDialog` 替代 `FolderBrowserDialog`
5. 设置默认文件夹和文件名（从任务当前的 `SaveFile` 属性获取）
6. 用户确认后更新任务的 `SaveFile` 属性
7. 保存数据并刷新表格

```csharp
/// <summary>
/// 事件处理：点击"修改保存文件"菜单项。
/// </summary>
private void 修改保存文件MToolStripMenuItem_Click(object sender, EventArgs e)
{
    if (dgv_语音生成_任务清单.SelectedRows.Count != 1)
    {
        return;
    }

    VoiceGenerationTask task = dgv_语音生成_任务清单.SelectedRows[0].DataBoundItem as VoiceGenerationTask;
    if (task == null)
    {
        return;
    }

    if (task.Status != VoiceGenerationTaskStatus.未开始 && 
        task.Status != VoiceGenerationTaskStatus.排队中)
    {
        MessageBox.Show(
            "只能修改\"未开始\"或\"排队中\"状态的任务。",
            "提示",
            MessageBoxButtons.OK,
            MessageBoxIcon.Information);
        return;
    }

    using (SaveFileDialog saveDialog = new SaveFileDialog())
    {
        saveDialog.Filter = "WAV文件|*.wav";
        saveDialog.Title = "选择保存文件";
        saveDialog.OverwritePrompt = true;

        if (!string.IsNullOrWhiteSpace(task.SaveFile))
        {
            string folder = Path.GetDirectoryName(task.SaveFile);
            string fileName = Path.GetFileName(task.SaveFile);
            if (!string.IsNullOrWhiteSpace(folder) && Directory.Exists(folder))
            {
                saveDialog.InitialDirectory = folder;
            }
            if (!string.IsNullOrWhiteSpace(fileName))
            {
                saveDialog.FileName = fileName;
            }
        }

        if (saveDialog.ShowDialog() == DialogResult.OK)
        {
            task.SaveFile = saveDialog.FileName;
            save语音生成任务清单Data();
            refresh语音生成任务清单DataGridView();
        }
    }
}
```

## 四、修改步骤

| 步骤 | 文件 | 修改内容 |
|------|------|----------|
| 1 | VoiceGenerationPanel.cs | 重命名事件处理方法 `修改存储文件夹MToolStripMenuItem_Click` → `修改保存文件MToolStripMenuItem_Click`，并重写方法实现 |
| 2 | VoiceGenerationPanel.Designer.cs | 更新事件绑定方法名 |
| 3 | VoiceGenerationPanel.cs | 修改菜单启用条件逻辑 |

## 五、验证要点

1. 选择单个"未开始"状态任务时，菜单应启用
2. 选择单个"排队中"状态任务时，菜单应启用
3. 选择其他状态任务时，菜单应禁用
4. 选择多个任务时，菜单应禁用
5. 点击菜单后弹出保存文件对话框，默认路径和文件名正确
6. 状态验证失败时弹出提示框
7. 修改后数据正确保存并刷新显示
