# 文本拆分选项保存到项目文件功能实现方案

## 一、需求概述

为"文本拆分"面板的"按句子拆分"、"按对话拆分"、"忽略换行符"三个选项添加保存到项目文件的功能，实现与"拆分长度"相同的加载优先级逻辑：

1. 优先加载项目文件中的值
2. 否则加载系统配置
3. 最后采用默认值（默认选"按句子拆分"）

**特殊要求**：默认项目文件不保存这些选项，也不要从默认项目文件加载这些选项。

## 二、现状分析

### 2.1 当前"拆分长度"的实现方式

**数据存储**：

* 项目文件：`ProjectData.TextSplit_SplitLength`（可空类型 `int?`）

* 系统配置：`Setting.ini` 中按引擎ID存储（键名：`TextSplit_SplitLength_{engineId}`）

**保存逻辑**（[TextSplitPanel.cs:123-131](file:///d:/User%20Data/guanw@hotmail.com/我的私有云/GW's%20Data/我的开发/GWs/TTL工具箱/TTL%20Tools%20Box%20V1.0/GW.TTLtoolsBox.WinFormUi/UI/Panels/TextSplitPanel.cs#L123-L131)）：

```csharp
public void SaveData(ProjectFile projectFile)
{
    if (projectFile != null)
    {
        projectFile.TextSplit_OriginalText = this.tb_原始文本.Text;
        projectFile.TextSplit_FinalText = this.tb_最终文本.Text;
        projectFile.TextSplit_SplitLength = (int)this.nud_拆分长度.Value;
    }
}
```

**加载逻辑**（[TextSplitPanel.cs:139-172](file:///d:/User%20Data/guanw@hotmail.com/我的私有云/GW's%20Data/我的开发/GWs/TTL工具箱/TTL%20Tools%20Box%20V1.0/GW.TTLtoolsBox.WinFormUi/UI/Panels/TextSplitPanel.cs#L139-L172)）：

```csharp
if (!keepSplitLength)
{
    int splitLength = 100;
    _isSplitLengthFromProject = false;

    if (!loadFromSystemConfig && projectFile.TextSplit_SplitLength.HasValue)
    {
        splitLength = projectFile.TextSplit_SplitLength.Value;
        _isSplitLengthFromProject = true;
    }
    else
    {
        splitLength = Setting.GetTextSplit_SplitLength(CurrentEngineId, 100);
        _isSplitLengthFromProject = false;
    }

    this.nud_拆分长度.Value = splitLength;
}
```

### 2.2 当前三个选项的存储方式

| 选项    | 当前存储位置      | 存储方式                                                   |
| ----- | ----------- | ------------------------------------------------------ |
| 按句子拆分 | 系统配置        | `Setting.SetValue(rb_拆分方式_按句子拆分.Name, ...)`            |
| 按对话拆分 | 系统配置        | `Setting.SetValue(rb_拆分参数_按对话拆分.Name, ...)`            |
| 忽略换行符 | 系统配置（按引擎ID） | `Setting.SetTextSplit_IgnoreLineBreaks(engineId, ...)` |

**需要改进**：拆分方式也需要按引擎ID存储，与"拆分长度"和"忽略换行符"保持一致。

### 2.3 默认项目文件的判断

在 [MainForm.cs:479-483](file:///d:/User%20Data/guanw@hotmail.com/我的私有云/GW's%20Data/我的开发/GWs/TTL工具箱/TTL%20Tools%20Box%20V1.0/GW.TTLtoolsBox.WinFormUi/MainForm.cs#L479-L483) 中：

```csharp
private bool isDefaultProjectFile()
{
    return string.IsNullOrEmpty(_currentProjectFilePath) ||
           _currentProjectFilePath.Equals(默认项目_文件, StringComparison.OrdinalIgnoreCase);
}
```

## 三、实现方案

### 3.1 数据结构修改

#### 3.1.1 修改 `Setting.cs`

添加按引擎ID存储拆分方式的方法：

```csharp
/// <summary>
/// 获取文本拆分的拆分方式（true=按句子拆分，false=按对话拆分）。
/// </summary>
/// <param name="engineId">TTL引擎ID。</param>
/// <param name="defaultValue">默认值。</param>
/// <returns>是否按句子拆分。</returns>
public static bool GetTextSplit_SplitBySentence(string engineId, bool defaultValue = true)
{
    if (string.IsNullOrEmpty(engineId))
    {
        return defaultValue;
    }

    string key = $"TextSplit_SplitBySentence_{engineId}";
    string value = GetValue(key, defaultValue.ToString());
    return bool.TryParse(value, out bool result) ? result : defaultValue;
}

/// <summary>
/// 设置文本拆分的拆分方式（true=按句子拆分，false=按对话拆分）。
/// </summary>
/// <param name="engineId">TTL引擎ID。</param>
/// <param name="value">是否按句子拆分。</param>
public static void SetTextSplit_SplitBySentence(string engineId, bool value)
{
    if (string.IsNullOrEmpty(engineId))
    {
        return;
    }

    string key = $"TextSplit_SplitBySentence_{engineId}";
    SetValue(key, value);
    Save();
}
```

#### 3.1.2 修改 `ProjectData.cs`

添加三个可空类型属性：

```csharp
/// <summary>
/// 获取文本拆分的拆分方式（true=按句子拆分，false=按对话拆分）。
/// </summary>
/// <param name="engineId">TTL引擎ID。</param>
/// <param name="defaultValue">默认值。</param>
/// <returns>是否按句子拆分。</returns>
public static bool GetTextSplit_SplitBySentence(string engineId, bool defaultValue = true)
{
    if (string.IsNullOrEmpty(engineId))
    {
        return defaultValue;
    }

    string key = $"TextSplit_SplitBySentence_{engineId}";
    string value = GetValue(key, defaultValue.ToString());
    return bool.TryParse(value, out bool result) ? result : defaultValue;
}

/// <summary>
/// 设置文本拆分的拆分方式（true=按句子拆分，false=按对话拆分）。
/// </summary>
/// <param name="engineId">TTL引擎ID。</param>
/// <param name="value">是否按句子拆分。</param>
public static void SetTextSplit_SplitBySentence(string engineId, bool value)
{
    if (string.IsNullOrEmpty(engineId))
    {
        return;
    }

    string key = $"TextSplit_SplitBySentence_{engineId}";
    SetValue(key, value);
    Save();
}
```

**说明**：

* "按句子拆分"和"按对话拆分"是互斥的单选项，用一个布尔属性表示即可

* 使用可空类型是为了区分"未设置"和"设置了具体值"两种状态

#### 3.1.3 修改 `ProjectFile.cs`

添加对应的属性访问器：

```csharp
/// <summary>
/// 获取或设置文本拆分的拆分方式（true=按句子拆分，false=按对话拆分）。
/// </summary>
public bool? TextSplit_SplitBySentence
{
    get { return _projectData.TextSplit_SplitBySentence; }
    set { _projectData.TextSplit_SplitBySentence = value; }
}

/// <summary>
/// 获取或设置文本拆分是否忽略换行符。
/// </summary>
public bool? TextSplit_IgnoreLineBreaks
{
    get { return _projectData.TextSplit_IgnoreLineBreaks; }
    set { _projectData.TextSplit_IgnoreLineBreaks = value; }
}
```

### 3.2 修改 `TextSplitPanel.cs`

#### 3.2.1 添加字段

```csharp
/// <summary>
/// 拆分方式是否来自项目文件。
/// </summary>
private bool _isSplitBySentenceFromProject = false;

/// <summary>
/// 忽略换行符设置是否来自项目文件。
/// </summary>
private bool _isIgnoreLineBreaksFromProject = false;
```

#### 3.2.2 修改 `SaveData` 方法

```csharp
public void SaveData(ProjectFile projectFile, bool isDefaultProject = false)
{
    if (projectFile != null)
    {
        projectFile.TextSplit_OriginalText = this.tb_原始文本.Text;
        projectFile.TextSplit_FinalText = this.tb_最终文本.Text;
        projectFile.TextSplit_SplitLength = (int)this.nud_拆分长度.Value;

        // 默认项目文件不保存拆分方式和忽略换行符设置
        if (!isDefaultProject)
        {
            projectFile.TextSplit_SplitBySentence = this.rb_拆分方式_按句子拆分.Checked;
            projectFile.TextSplit_IgnoreLineBreaks = this.cb_文本拆分_按句子拆分_忽略换行符.Checked;
        }
        else
        {
            // 清空默认项目文件中的这些设置
            projectFile.TextSplit_SplitBySentence = null;
            projectFile.TextSplit_IgnoreLineBreaks = null;
        }
    }
}
```

#### 3.2.3 修改 `LoadData` 方法

```csharp
public void LoadData(ProjectFile projectFile, bool loadFromSystemConfig = false, bool keepSplitLength = false, bool isDefaultProject = false)
{
    if (projectFile != null)
    {
        _isLoading = true;
        UpdateUi(() =>
        {
            this.tb_原始文本.Text = projectFile.TextSplit_OriginalText;
            this.tb_最终文本.Text = projectFile.TextSplit_FinalText;

            if (!keepSplitLength)
            {
                int splitLength = 100;
                _isSplitLengthFromProject = false;

                if (!loadFromSystemConfig && projectFile.TextSplit_SplitLength.HasValue)
                {
                    splitLength = projectFile.TextSplit_SplitLength.Value;
                    _isSplitLengthFromProject = true;
                }
                else
                {
                    splitLength = Setting.GetTextSplit_SplitLength(CurrentEngineId, 100);
                    _isSplitLengthFromProject = false;
                }

                this.nud_拆分长度.Value = splitLength;
            }

            // 加载拆分方式（按句子/按对话）
            bool splitBySentence = true; // 默认值
            _isSplitBySentenceFromProject = false;

            // 默认项目文件不从项目文件加载这些设置
            if (!isDefaultProject && !loadFromSystemConfig && projectFile.TextSplit_SplitBySentence.HasValue)
            {
                splitBySentence = projectFile.TextSplit_SplitBySentence.Value;
                _isSplitBySentenceFromProject = true;
            }
            else
            {
                // 从系统配置加载（按引擎ID）
                splitBySentence = Setting.GetTextSplit_SplitBySentence(CurrentEngineId, true);
                _isSplitBySentenceFromProject = false;
            }

            this.rb_拆分方式_按句子拆分.Checked = splitBySentence;
            this.rb_拆分参数_按对话拆分.Checked = !splitBySentence;

            // 加载忽略换行符设置
            bool ignoreLineBreaks = true; // 默认值
            _isIgnoreLineBreaksFromProject = false;

            // 默认项目文件不从项目文件加载这些设置
            if (!isDefaultProject && !loadFromSystemConfig && projectFile.TextSplit_IgnoreLineBreaks.HasValue)
            {
                ignoreLineBreaks = projectFile.TextSplit_IgnoreLineBreaks.Value;
                _isIgnoreLineBreaksFromProject = true;
            }
            else
            {
                // 从系统配置加载
                ignoreLineBreaks = Setting.GetTextSplit_IgnoreLineBreaks(CurrentEngineId, true);
                _isIgnoreLineBreaksFromProject = false;
            }

            this.cb_文本拆分_按句子拆分_忽略换行符.Checked = ignoreLineBreaks;
        });
        _isLoading = false;
    }
}
```

#### 3.2.4 修改事件处理方法

修改 `rb_拆分方式_CheckedChanged` 方法，改为按引擎ID保存到系统配置：

```csharp
private void rb_拆分方式_CheckedChanged(object sender, EventArgs e)
{
    if (!_isLoading)
    {
        Setting.SetTextSplit_SplitBySentence(CurrentEngineId, this.rb_拆分方式_按句子拆分.Checked);
    }
    refresh文本拆分Ui();
}
```

修改 `bt_开始拆分_Click` 方法，移除旧的保存逻辑（因为已经在 `rb_拆分方式_CheckedChanged` 中处理）：

```csharp
private void bt_开始拆分_Click(object sender, EventArgs e)
{
    do拆分文本Job();
    refresh文本拆分Ui();
}
```

### 3.3 修改 `MainForm.cs`

#### 3.3.1 修改 `save文本拆分Data` 方法

```csharp
private void save文本拆分Data()
{
    if (_textSplitPanel != null)
    {
        _textSplitPanel.CurrentEngineId = getCurrentEngineId();
        _textSplitPanel.SaveData(getProjectFile(), isDefaultProjectFile());
    }
}
```

#### 3.3.2 修改 `load文本拆分` 方法

```csharp
private void load文本拆分(bool loadSplitLengthFromSystemConfig = false, bool keepSplitLength = false)
{
    if (_textSplitPanel != null)
    {
        _textSplitPanel.CurrentEngineId = getCurrentEngineId();
        _textSplitPanel.LoadData(getProjectFile(), loadSplitLengthFromSystemConfig, keepSplitLength, isDefaultProjectFile());
    }
}
```

## 四、修改文件清单

| 文件                                                     | 修改内容                                                                           |
| ------------------------------------------------------ | ------------------------------------------------------------------------------ |
| `GW.TTLtoolsBox.WinFormUi\Helper\Setting.cs`           | 添加 `GetTextSplit_SplitBySentence` 和 `SetTextSplit_SplitBySentence` 方法（按引擎ID存储） |
| `GW.TTLtoolsBox.Core\Entity\ProjectData.cs`            | 添加 `TextSplit_SplitBySentence` 和 `TextSplit_IgnoreLineBreaks` 属性               |
| `GW.TTLtoolsBox.Core\FileAccesser\ProjectFile.cs`      | 添加对应的属性访问器                                                                     |
| `GW.TTLtoolsBox.WinFormUi\UI\Panels\TextSplitPanel.cs` | 修改 `SaveData` 和 `LoadData` 方法，添加相关字段，修改事件处理方法                                  |
| `GW.TTLtoolsBox.WinFormUi\MainForm.cs`                 | 修改 `save文本拆分Data` 和 `load文本拆分` 方法调用                                            |

## 五、加载优先级总结

| 场景       | 拆分方式来源      | 忽略换行符来源     |
| -------- | ----------- | ----------- |
| 用户项目文件有值 | 项目文件        | 项目文件        |
| 用户项目文件无值 | 系统配置（按引擎ID） | 系统配置（按引擎ID） |
| 默认项目文件   | 系统配置（按引擎ID） | 系统配置（按引擎ID） |
| 全部无值     | 默认值（按句子拆分）  | 默认值（true）   |

## 六、注意事项

1. **向后兼容性**：新增的属性使用可空类型，旧版本项目文件加载时这些属性为 null，不会影响现有功能。

2. **系统配置同步**：用户在UI中修改这些选项时，仍然会同步保存到系统配置（当前行为保持不变），确保下次创建新项目时能使用最近的设置。

3. **默认项目文件的特殊处理**：

   * 保存时：不保存这些选项到项目文件，或显式设置为 null

   * 加载时：跳过项目文件，直接从系统配置加载

