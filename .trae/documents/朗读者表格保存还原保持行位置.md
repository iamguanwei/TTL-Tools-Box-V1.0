# 朗读者表格保存/还原后保持行位置方案

## 问题分析

当前代码中，点击"保存"或"还原"按钮后，表格会重新绑定数据源，导致滚动位置丢失。

### 问题代码位置

**保存按钮** ([TtlSchemePanel.cs:1324-1345](file:///d:/User%20Data/guanw@hotmail.com/我的私有云/GW's%20Data/我的开发/GWs/TTL工具箱/TTL%20Tools%20Box%20V1.0/GW.TTLtoolsBox.WinFormUi/UI/Panels/TtlSchemePanel.cs#L1324-L1345))：
```csharp
private void bt_TTL方案_保存配置_Click(object sender, EventArgs e)
{
    // ... 保存逻辑
    createSpeakersFromRoles(currentEngine);  // 这里会重新绑定数据源
    // ...
}
```

**还原按钮** ([TtlSchemePanel.cs:1352-1361](file:///d:/User%20Data/guanw@hotmail.com/我的私有云/GW's%20Data/我的开发/GWs/TTL工具箱/TTL%20Tools%20Box%20V1.0/GW.TTLtoolsBox.WinFormUi/UI/Panels/TtlSchemePanel.cs#L1352-L1361))：
```csharp
private void bt_TTL方案_还原配置_Click(object sender, EventArgs e)
{
    // ...
    cb_TTL方案_当前方案名称_SelectedIndexChanged(sender, e);  // 重新加载整个方案
    // ...
}
```

**数据绑定方法** ([TtlSchemePanel.cs:904-926](file:///d:/User%20Data/guanw@hotmail.com/我的私有云/GW's%20Data/我的开发/GWs/TTL工具箱/TTL%20Tools%20Box%20V1.0/GW.TTLtoolsBox.WinFormUi/UI/Panels/TtlSchemePanel.cs#L904-L926))：
```csharp
private void bindSpeakersToGrid(ITtlEngineConnector ttl, bool forceRefresh = false)
{
    // ...
    this.dgv_TTL方案_朗读者参数配置.DataSource = speakerArray;  // 直接设置DataSource，滚动位置丢失
    // ...
}
```

## 解决方案

在 `bindSpeakersToGrid` 方法中添加滚动位置保存和恢复逻辑：

1. 在重新绑定数据源之前，保存当前的第一行显示索引（`FirstDisplayedScrollingRowIndex`）
2. 在重新绑定数据源之后，恢复滚动位置

### 实现步骤

#### 步骤1：修改 `bindSpeakersToGrid` 方法

在 [TtlSchemePanel.cs](file:///d:/User%20Data/guanw@hotmail.com/我的私有云/GW's%20Data/我的开发/GWs/TTL工具箱/TTL%20Tools%20Box%20V1.0/GW.TTLtoolsBox.WinFormUi/UI/Panels/TtlSchemePanel.cs) 的 `bindSpeakersToGrid` 方法中：

```csharp
private void bindSpeakersToGrid(ITtlEngineConnector ttl, bool forceRefresh = false)
{
    var speakerArray = ttl.Speakers.Select(s => s.Clone()).ToArray();

    if (!forceRefresh && !hasSpeakerDataChanged(speakerArray))
    {
        return;
    }

    // 保存当前滚动位置
    int scrollRowIndex = this.dgv_TTL方案_朗读者参数配置.FirstDisplayedScrollingRowIndex;

    if (speakerArray != null && speakerArray.Length > 0)
    {
        setupSpeakerGridColumns();
        this.dgv_TTL方案_朗读者参数配置.DataSource = speakerArray;
        this.dgv_TTL方案_朗读者参数配置.AutoResizeColumns(DataGridViewAutoSizeColumnsMode.AllCells);

        // 恢复滚动位置
        if (scrollRowIndex >= 0 && scrollRowIndex < speakerArray.Length)
        {
            this.dgv_TTL方案_朗读者参数配置.FirstDisplayedScrollingRowIndex = scrollRowIndex;
        }
    }
    else
    {
        this.dgv_TTL方案_朗读者参数配置.DataSource = null;
    }

    OnSpeakersChanged();
    update自动生成全部朗读者预览音频按钮状态(true);
}
```

### 涉及文件

| 文件 | 修改内容 |
|------|----------|
| [TtlSchemePanel.cs](file:///d:/User%20Data/guanw@hotmail.com/我的私有云/GW's%20Data/我的开发/GWs/TTL工具箱/TTL%20Tools%20Box%20V1.0/GW.TTLtoolsBox.WinFormUi/UI/Panels/TtlSchemePanel.cs) | 修改 `bindSpeakersToGrid` 方法，添加滚动位置保存和恢复逻辑 |

### 注意事项

1. `FirstDisplayedScrollingRowIndex` 在表格为空或没有数据时可能为 -1 或 0，需要做边界检查
2. 恢复滚动位置时需要确保索引不超出新数据数组的范围
3. 此修改同时解决了"保存"和"还原"两个场景的问题，因为它们都调用了 `bindSpeakersToGrid` 方法
