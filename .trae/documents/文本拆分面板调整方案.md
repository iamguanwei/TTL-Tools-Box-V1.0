# 文本拆分面板调整方案

## 一、需求概述

1. 删除"按空行拆分"单选按钮UI及相关代码
2. 调整"按对话拆分"单选按钮的位置到原"按空行拆分"的位置
3. 为"按句子拆分"的处理逻辑增加预处理方法

---

## 二、详细实施步骤

### 步骤1：删除"按空行拆分"单选按钮UI及相关代码

#### 1.1 修改 TextSplitPanel.Designer.cs

**删除控件声明：**
- 第44行：`this.rb_拆分参数_按空行拆分 = new System.Windows.Forms.RadioButton();`
- 第209行：`this.gb_拆分参数.Controls.Add(this.rb_拆分参数_按空行拆分);`
- 第234-242行：控件属性设置代码块
- 第363行：`private System.Windows.Forms.RadioButton rb_拆分参数_按空行拆分;`

#### 1.2 修改 TextSplitPanel.cs

**删除以下代码：**
- 第199行：`this.rb_拆分参数_按空行拆分.Checked = bool.Parse(Setting.GetValue(this.rb_拆分参数_按空行拆分.Name, "false"));`
- 第208行：`this.rb_拆分参数_按空行拆分.CheckedChanged += rb_拆分方式_CheckedChanged;`
- 第301-304行：业务逻辑分支（if判断块）
- 第440行：`Setting.SetValue(this.rb_拆分参数_按空行拆分.Name, this.rb_拆分参数_按空行拆分.Checked.ToString());`

#### 1.3 保留 TextSplitHelper.cs 中的 SplitByEmptyLine 方法

该方法为public方法，虽然当前仅被TextSplitPanel使用，但作为工具类方法保留以备将来使用。

---

### 步骤2：调整"按对话拆分"单选按钮的位置

#### 2.1 修改 TextSplitPanel.Designer.cs

将 `rb_拆分参数_按对话拆分` 的 Location 从 `(6, 150)` 改为 `(6, 111)`。

---

### 步骤3：为"按句子拆分"增加预处理方法

#### 3.1 在 TextSplitHelper.cs 中添加预处理方法

新增方法 `PreprocessText`，实现以下功能：

```csharp
/// <summary>
/// 预处理文本，为按句子拆分做准备。
/// </summary>
/// <param name="originalText">原始文本。</param>
/// <param name="ignoreLineBreaks">是否忽略换行符（启用空行+1处理）。</param>
/// <returns>预处理后的文本。</returns>
/// <remarks>
/// 处理规则：
/// 1. 去除每一行的前导和后续空格
/// 2. 若ignoreLineBreaks为true，对连续空行进行+1处理：
///    - "第一段文字<br><br>第二段文字" → "第一段文字<br><br><br>第二段文字"
///    - "第一段文字<br><br><br>第二段文字" → "第一段文字<br><br><br><br>第二段文字"
/// </remarks>
public static string PreprocessText(string originalText, bool ignoreLineBreaks)
```

**处理逻辑：**
1. 按换行符拆分文本为行数组
2. 对每一行执行 Trim() 操作
3. 若 ignoreLineBreaks 为 true：
   - 遍历处理后的行数组
   - 对于连续出现的空行，在原位置后额外插入一个空行
4. 重新拼接为文本返回

#### 3.2 修改 TextSplitPanel.cs 中的 do拆分文本Job 方法

在调用 `SplitText` 或 `SplitTextIgnoreLineBreaks` 之前，先调用 `PreprocessText` 进行预处理。

修改后的逻辑：

```csharp
if (this.rb_拆分方式_按句子拆分.Checked == true)
{
    bool ignoreLineBreaks = this.cb_文本拆分_按句子拆分_忽略换行符.Checked;
    
    // 预处理文本
    string preprocessedText = TextSplitHelper.PreprocessText(paragraph, ignoreLineBreaks);
    
    if (ignoreLineBreaks)
    {
        processedText = TextSplitHelper.SplitTextIgnoreLineBreaks(
            preprocessedText,
            (int)分割_最小长度,
            (int)this.nud_拆分长度.Value,
            整句_分割符号.ToCharArray(),
            半句_分割符号.ToCharArray());
    }
    else
    {
        processedText = TextSplitHelper.SplitText(
            preprocessedText,
            (int)分割_最小长度,
            (int)this.nud_拆分长度.Value,
            整句_分割符号.ToCharArray(),
            半句_分割符号.ToCharArray());
    }
}
```

---

## 三、涉及文件清单

| 文件 | 修改类型 | 修改内容 |
|------|----------|----------|
| TextSplitPanel.Designer.cs | 删除+修改 | 删除"按空行拆分"控件，调整"按对话拆分"位置 |
| TextSplitPanel.cs | 删除+修改 | 删除相关代码，修改业务逻辑调用预处理方法 |
| TextSplitHelper.cs | 新增 | 添加 PreprocessText 预处理方法 |

---

## 四、UI布局变化

### 修改前：
```
gb_拆分参数 (GroupBox)
├── rb_拆分方式_按句子拆分 - 位置(6, 27)
├── cb_文本拆分_按句子拆分_忽略换行符 - 位置(24, 73)
├── rb_拆分参数_按空行拆分 - 位置(6, 111)  ← 待删除
├── rb_拆分参数_按对话拆分 - 位置(6, 150)  ← 待移动
├── lab_拆分长度 / nud_拆分长度 / lab_拆分长度说明
├── bt_开始拆分
└── bt_清理所有文本
```

### 修改后：
```
gb_拆分参数 (GroupBox)
├── rb_拆分方式_按句子拆分 - 位置(6, 27)
├── cb_文本拆分_按句子拆分_忽略换行符 - 位置(24, 73)
├── rb_拆分参数_按对话拆分 - 位置(6, 111)  ← 移动到此
├── lab_拆分长度 / nud_拆分长度 / lab_拆分长度说明
├── bt_开始拆分
└── bt_清理所有文本
```

---

## 五、预处理方法示例

**输入：**
```
第一段文字

第二段文字
```
（中间有2个空行）

**输出（ignoreLineBreaks=true）：**
```
第一段文字


第二段文字
```
（中间有3个空行，即原空行数+1）
